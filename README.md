# assistant-sync

**Leave work at work. Find it at home.**

`assistant-sync` lets your AI assistant at work pack up notes, decisions, and context as a signed
packet and deliver it to your home instance — automatically, asynchronously, no shared server
required. When you open Claude at home, it's there.

See `projects/assistant-sync/` in your guild workspace for full design docs.
